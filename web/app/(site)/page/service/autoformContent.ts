/**
 * 오토폼 장의 자료. 실제 제품 화면 세 장(public/screens/solution-form-automation-*.jpg — 사용자가
 * 준 자산 저장소에서 받은 것)에 **찍혀 있는 글·숫자만** 옮겼다. 제품이 로컬에 떠 있지 않아
 * (:3301~:3312 는 전부 API 라 「Cannot GET /」) 새로 찍은 화면은 없다. 2026-09-22.
 *
 * 화면에서 읽은 것(그대로): 데이터베이스 dmine · 테이블 15개 · 생성된 툴 38개 · 분석된 양식 9개,
 * 양식 목록의 파일명·쪽·표·스칼라·리스트 수, 「hwp·hwpx 파일을 선택하세요 — 라벨·리스트를 자동
 * 감지하고 매핑 초안을 만들어 드려요」, 매핑 화면의 「제품명 ← name」 같은 대응, 「사용자 확정 필요
 * 42건」, 문서 생성 창의 「이 양식은 이렇게 채워집니다」 · 레코드 여섯(dm-ai-01~06) · 넘침 처리
 * 두 갈래 · 「생성 실행 (dm-ai-04)」. 매핑 화면의 빈양식은 스칼라 40(등록 목록의 같은 파일명은 25) —
 * 다른 분석 회차다. 카드 meta 는 목록 값을 쓴다. 오류가 아니다.
 *
 * **우리가 쓴 문장**(사용자 확인 대상): AF_DEMO 의 단계 이름 셋과 설명(sub) 셋, AF_BEFORE 의
 * 말풍선 셋·제목·✕ 셋, AF_AFTER 의 제목·✓ 셋, AF_FORMS 의 탭 제목(t2)·설명(d) 셋,
 * 그리고 page.tsx 의 큰 문장·설명·히어로 문구. 제품 주장은 solutionContent.ts 의 AUTOFORM
 * (받은 프로토타입 문장) 범위를 넘지 않는다.
 */

/** 시연 — 화면 세 장을 차례로. 라벨은 그 화면에 찍힌 값. */
export const AF_DEMO = {
  steps: [
    {
      t: '양식 등록',
      sub: '쓰던 hwp·hwpx 를 그대로 올리면 표와 칸을 읽습니다',
      shot: { src: '/screens/solution-form-automation-upload.jpg', alt: '양식 등록 화면 — 데이터베이스 연결 상태와 분석된 양식 9개 목록', w: 1600, h: 1121 },
      pos: '50% 0%',
      labels: ['분석된 양식 9개', '품질관리기록서_코스메틱_빈양식.hwpx · 2쪽 · 표 5 · 스칼라 25 · 리스트 2', '데이터베이스 dmine · 테이블 15개 · 생성된 툴 38개'],
    },
    {
      t: '칸과 데이터 맺기',
      sub: '원본을 띄워 둔 채 칸마다 어느 값이 들어갈지 정합니다',
      shot: { src: '/screens/solution-form-automation-mapping.jpg', alt: '매핑 화면 — 한글 원본 위에 칸별 대응(제품명 ← name 등)이 표시된다', w: 1600, h: 799 },
      pos: '50% 50%',
      labels: ['제품명 ← name', '제조번호 ← documentNo', '제조량 ← totalCapacity', '시험담당자 ← auditor', '사용자 확정 필요 42건'],
    },
    {
      t: '레코드 고르면 문서 생성',
      sub: '레코드 하나를 고르면 딸린 값과 품목 목록까지 찾아 채웁니다',
      shot: { src: '/screens/solution-form-automation-generate.jpg', alt: '문서 생성 창 — 레코드 목록에서 dm-ai-04 를 고르고 넘침 처리 방식을 정한다', w: 1600, h: 1024 },
      pos: '50% 40%',
      labels: ['이 양식은 이렇게 채워집니다', 'dm-ai-04 · 협력업체 정기 평가', '용량 초과 시 문서를 만들지 않고 알림 (기본)', '생성 실행 (dm-ai-04)'],
    },
  ],
}

/** 전/후 — 왼쪽은 지금 문서를 만드는 방식(우리가 쓴 말), 오른쪽은 매핑 화면. */
export const AF_BEFORE = {
  title: '같은 문서를 매번 처음부터 씁니다',
  bubbles: [
    { who: 'me', t: '지난달 품질관리기록서 파일 어디 있어요? 그거 복사해서 쓰게' },
    { who: 'them', t: '제조번호랑 제조량은 MES 에서 다시 찾아서 붙여 넣어야 해요' },
    { who: 'me', t: '검사항목 표가 넘쳐서 두 쪽으로 밀렸는데 서식 깨졌어요…' },
  ],
  points: ['값을 찾아 복사해 넣는 손이 문서마다 든다', '양식을 새로 만들면 부서마다 서식이 달라진다', '표가 넘치면 서식이 깨진 채 나간다'],
}
export const AF_AFTER = {
  title: '레코드 하나 고르면 서식 그대로 나옵니다',
  points: [
    '쓰던 hwp·hwpx 원본을 그대로 등록 — 「라벨·리스트를 자동 감지하고 매핑 초안을 만들어 드려요」',
    '「제품명 ← name」처럼 칸과 데이터를 한 번 맺어 두면 다음부터는 고르기만',
    '넘치면 「문서를 만들지 않고 알림」 또는 「행을 늘려서 전부 넣기」 — 깨진 채 나가지 않는다',
  ],
}

/** 양식 셋 — 등록 화면의 목록에 있는 것. 값은 그 줄에 찍힌 그대로. */
export const AF_DB = { name: 'dmine', tables: '15개', tools: '38개', forms: '9개' }
export const AF_FORMS = [
  { k: 'quote', v: '견적서', t2: '견적서 — 품목이 줄로 늘어나는 양식', d: '표 8개에 품목 리스트 1개. 품목 수가 문서마다 달라도 같은 서식으로.',
    file: '견적서_코스메틱_양식.hwpx', kind: 'HWPX', meta: '1쪽 · 표 8 · 스칼라 20 · 리스트 1', when: '2026. 7. 23. 오후 3:56:05', pairs: [] as [string, string][] },
  { k: 'qc', v: '품질관리기록서', t2: '품질관리기록서 — 두 쪽짜리 검사 기록', d: '표 5개에 검사항목 리스트 2개(벌크·완제품). 채워진 문서를 올려도 서식을 읽는다.',
    file: '품질관리기록서_코스메틱.hwp', kind: 'HWP', meta: '2쪽 · 표 5 · 스칼라 30 · 리스트 2', when: '2026. 7. 21. 오후 1:23:18', pairs: [] as [string, string][] },
  // 칸 대응 넷은 매핑 화면(머리: 품질관리기록서_코스메틱_빈양식.hwpx)에 찍힌 것 — 이 파일에만 붙인다.
  { k: 'blank', v: '빈양식', t2: '빈양식 — 값이 하나도 없어도 읽는다', d: '빈 서식을 올려도 표와 칸을 같은 방식으로 인식하고, 제조기록서 ID 하나로 채운다.',
    file: '품질관리기록서_코스메틱_빈양식.hwpx', kind: 'HWPX', meta: '2쪽 · 표 5 · 스칼라 25 · 리스트 2', when: '2026. 7. 21. 오전 2:09:54',
    pairs: [['제품명', 'name'], ['제조번호', 'documentNo'], ['제조량', 'totalCapacity'], ['시험담당자', 'auditor']] as [string, string][] },
]
