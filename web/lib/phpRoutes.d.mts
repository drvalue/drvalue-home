/** phpRoutes.mjs 는 next.config.mjs 가 읽어야 해서 .mjs 다. 타입만 여기 적는다. */
export declare const CLEAN_PATHS: readonly string[]
export declare const PHP_REDIRECTS: readonly { source: string; destination: string; permanent: boolean }[]
