
# OS
.DS_Store

# Next 이관 작업본
web/node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo

# Nest API
api/node_modules/
api/dist/
.env
api/.env

# CMS (Directus)
cms/.env
web/.env
cms/public-endpoints.json

# next dev 가 web/AGENTS.md 끝에 자기 블록을 다시 붙인다. 예전엔 파일 전체를
# 무시했는데, 이제 이 파일에 손으로 쓴 안내가 들어 있어서 무시하면 내려받은
# 사람에게 그 안내가 없다. 그래서 추적한다 — 블록째 커밋해 두면 next dev 가
# 같은 블록을 다시 써도 diff 가 안 생긴다.

# 작업용 로컬 폴더 — 커밋하지 않는다
.dryforge/

# 이 둘은 저장소에 안 올린다. 배포가 저장소를 웹 루트에 통째로 올리므로
# (deploy-ssh.yml 의 rsync 에 docs 제외가 없다) 커밋하면 공개 주소로 열린다.
# 값은 없지만 어디를 치면 되는지가 적혀 있다. 사본은 작업자끼리 따로 넘긴다.
docs/security.md
docs/tracking/findings.md
design/

# 업로드 파일(컨테이너 밖 bind mount). 증서·첨부가 들어 있다 — 저장소에 넣지 않는다.
data/
