# 지금 어디까지 왔나

## 목표

운영 중인 PHP 사이트를 **새 서버 위의 React 화면 + Nest 백엔드 + 자체 관리 화면**으로 바꾼다.
날짜를 정해 한 번에 바꾼다 — PHP 와 새 사이트를 나란히 돌리지 않는다.

## 된 것

| 덩어리 | 상태 | 근거 |
|---|---|---|
| 공개 화면 이관 (17장) | 됨 · 확인함 | `next build` 통과, 옛 주소 308 동작 |
| 새로 만든 화면 8장 (M.AX 4 · 서비스 4) + AI솔루션 되살림 | 됨 · 확인함 | `check-pages.py` 54/54 — 본문·그림 바닥, 안 쓰는 화면 0 |
| 홈 개편 (머리 그림 · 신뢰의 근거 · 사업 · 소식 목록 · 문의 띠 — 원본 구역은 하나도 안 남았다) | 됨 · 확인함 | `check-home.py` 23/23 — 원본을 더 안 부른다. Nest(:3500)는 떠 있어야 한다 |
| 상단 탭 막대 재설계 | 됨 · 확인함 | `check-header.py` 104/104 |
| 왼쪽 차례표 (8장) | 됨 · 확인함 | 붙은 장 목록을 응답에서 확인 |
| 문의 모달 접근성 | 됨 · 확인함 | `check-a11y.py` 266/266 |
| Nest 공개 API (게시판·문의) | 됨 · 확인함 | 2026-09-22 bmes 모양으로 재구성, DB 직결(TypeORM). `verify.sh` 55/55 |
| 특허·저작권·수행실적·연혁 편집 | 됨 · 확인함 | posts 의 board 로 33건. web 4장 + 홈 소식이 요청마다 읽음 — 저장 즉시 반영 실측(제목 PUT → grep 1 → 원복 0). `check-pages.py` 98/98 |
| 관리 화면 `/admin` (Directus 대체) | 됨 · **브라우저 왕복은 판정 불가** | 2026-09-22 결정 0014. web `/admin` + api `/api/admin/*`. tsc·build 통과, curl 로 화면·401 확인. 사용자가 IAM 으로 실제 로그인·편집을 눌러 봐야 한다 |
| 관리자 인가 (M.AX root 표) | 코드 됨 · **실제 표 대조는 판정 불가** | `ADMIN_MAX_DB_*` 값이 이 기계에 없다. 판정 함수 테스트 11/11. 값이 오면 로그인 한 번이 증거 |
| 운영 배포 통로 | 있음 · 안 써 봄 | GitHub Actions 수동 실행. 이 저장소에서 아직 안 눌렀다 |

## 남은 것

우선순위 순이다.

1. **특허 증서 2장의 운영본을 바꾼다.** 새 사이트는 개인정보 칸을 덮은 사본을
   걸었다(2026-09-18). 운영 서버의 원본은 아직 그대로 열려 있다.
2. **게시판 API 해시 노출을 게이트웨이 쪽에 알린다.**
3. **api · web · db 를 새 서버에 올린다.** 올릴 곳이 정해져야 그다음이
   전부 움직인다. 관리 화면 콜백 주소를 IAM 화이트리스트에, M.AX DB 읽기 계정을 `.env` 에.
4. **전환 날짜를 정한다.** 정해지면 당일에 게시판 글을 다시 옮긴다.
5. ~~디자인 통일~~ — 2026-09-18 끝. 공개 장 전부 새 틀(SolutionShell + data-rv).
   옛 꾸밈은 숨김 장 patent_old 하나만 남았다.
6. 화면 채우기 — 연혁 설명 8건, 한건 그림 1장, 규제검증 교체 그림 1장.
   전부 자료가 와야 한다.
7. 게시판 글 5건(공지 3·보도 2)은 로컬 DB 에 있다. 2026-09-18 문체를
   사람 말로 다시 썼다(이모지·체크리스트·「앞으로도 ~해 나가겠습니다」 제거,
   사실·URL 그대로). 운영 DB 가 서면(3번) 거기에도 넣어야 한다. 윤문본과 넣는 스크립트는
   저장소 밖 `~/drvalue-private/board_posts_윤문본.md`·`apply_posts.py` 에 있다(저장소는 공개 배포라 안 넣는다).

## 막힌 것

- **M.AX(nxcms) DB 읽기 계정이 없다.** 관리자 인가의 원본인데 값이 Doppler·운영에만
  있다. 없는 동안은 IAM 그룹(`ADMIN_IAM_GROUP`) 판정으로 돈다.
- **로컬 PHP 도커가 안 뜬다.** 디스크가 차 있다. 원본 대조가 필요하면
  운영본을 원본 삼아 돌린다.
